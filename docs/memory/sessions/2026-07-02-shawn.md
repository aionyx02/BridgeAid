---
type: session_log
status: archive
priority: p3
updated: 2026-07-02
context_policy: historical
owner: shawn
---

# Session Log: 2026-07-02 - Shawn

## Summary

- Maintainer 確認下一階段方向：Demo 體驗優先、本地 Ollama（ADR-0004 accepted）、提醒送達改輕量 in-process 排程（新 ADR-0006 proposed）、DB 用 Docker Postgres。
- 完成 TASK.010（OllamaIntentParser）、TASK.011（提醒送達排程）、TASK.012（Docker Postgres 落地 + Demo UI 打磨）。

## Actions Taken

- ADR-0004 / ADR-0005 frontmatter 與內文狀態統一為 accepted（原 frontmatter proposed、內文 accepted 不一致）；docs:sync 重生成 decisions.md。
- 新增 `backend/app/conversation/ollama_intent.py`：`OllamaIntentParser` 實作 `IntentParser` port。Ollama `/api/chat` + structured output（JSON schema）、白名單/型別/枚舉清洗、布林只設 True、age 1–120；失敗/逾時 fallback deterministic；成功時 deterministic 關鍵字命中優先（12 組回歸情境在 LLM 開啟下仍保證）。`build_intent_parser(settings)` 依 `INTENT_PARSER=ollama` 切換，預設 deterministic。config 新增 `INTENT_PARSER`、`OLLAMA_HOST`、`OLLAMA_MODEL`。
- 新增 ADR-0006（proposed）：提醒送達用 asyncio 背景迴圈（FastAPI lifespan，30s 掃描），取代 Redis/Celery（Windows 不支援、demo 過重）。`backend/app/reminder_delivery.py`：`deliver_due` 純函式 + `LineOrLogSender`（LINE push 或模擬送達）+ `run_scheduler`；`ReminderStore` 增 `list_pending()`；`line/client.py` 增 `push_text`（Push API）。送達失敗留 pending 重試。
- 新增 `docker-compose.yml`：postgres:17-alpine、綁 127.0.0.1、schema.sql 掛 initdb 首次自動套用、named volume。README 更新 Docker DB / Ollama 設定 / 端點與狀態章節。
- Demo UI 打磨（純 HTML，無新依賴）：提醒狀態中文徽章（待送達/已送達/已取消）+ 15s 輪詢展示到期送達、送出中 disabled 狀態、衝突訊息用服務名稱、consent 未勾提示、來源 dialog dl 排版。
- `ConversationManager._result_reply` 衝突提示改顯示服務中文名稱（原本輸出 raw service id，LINE/Web 都受益）。

## Commands / Validation

```bash
uv run ruff check .   # pass
uv run pytest         # 114 passed（原 93 + Ollama 11 + delivery 10）
docker compose up -d  # postgres healthy，9 張表自動建立
uv run python -m app.importer  # imported 6 services, 15 documents, 2 conflicts
```

Result: pass

## Detailed Notes

- 端到端實測（uvicorn + Docker Postgres）：/healthz ok；/chat 兩輪對話到推薦（急難救助、租金補貼、社宅、失業給付 + 擇一衝突提示已顯示中文名稱）；建立過期提醒（channel=email）後 30s 內由排程轉 `sent`；`/demo/` 靜態檔 200。
- PowerShell 5.1 `Invoke-RestMethod` 傳中文 JSON 需手動 UTF-8 bytes（否則 body 亂碼、關鍵字抽取全失敗）——僅測試腳本問題，非應用 bug。
- Docker Desktop 未啟動時 compose 會失敗（pipe not found）；啟動後即成。
- healthz 顯示 line_configured=true：keychain 已有 LINE channel secret，LINE 實連只差公開 webhook URL（ngrok/cloudflared）與 access token 確認。
- Ollama 實機驗證未做（本機未裝 Ollama）；程式以 MockTransport 測試。maintainer 裝好後：`ollama pull qwen3:4b` + `BRIDGEAID_INTENT_PARSER=ollama` 起服務即可。

## Completed Markers

## COMPLETED: TASK.010 - OllamaIntentParser（ADR-0004）：本地 LLM 欄位抽取 + 白名單清洗 + deterministic fallback，預設關閉
## COMPLETED: TASK.011 - 提醒到期送達（ADR-0006 proposed）：lifespan asyncio 排程 + LINE push/模擬送達，失敗重試
## COMPLETED: TASK.012 - Docker Postgres 落地（compose + initdb schema + importer 實跑）與 Demo UI 打磨
