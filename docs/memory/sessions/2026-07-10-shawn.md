---
type: session_log
status: archive
priority: p3
updated: 2026-07-10
context_policy: historical
owner: shawn
---

# Session Log: 2026-07-10 - Shawn

## Summary

- 為黑客松新增 Cloudflare Containers 部署設定：一個薄 Worker 前置、把現有 uvicorn app 打包成容器執行。應用碼零改動。
- 確認資料庫策略：**先不用 Cloudflare D1**（SQLite，與現行 psycopg/Postgres 方言不相容；其 REST API 僅適合管理用途）。之後要持久化走 Hyperdrive + 外部 Postgres 實作既有 Store seam。

## Actions Taken

- 新增 `Dockerfile`（`python:3.14-slim` + uv frozen install，保留 repo 佈局供 `data/`、`demo/` 路徑解析，`PYTHON_KEYRING_BACKEND=null` 讓 secret 走 env fallback，uvicorn 綁 `0.0.0.0:8080`）。
- 新增 `.dockerignore`、`worker/index.ts`（`Container` 子類 `sleepAfter=20m`、`getContainer` 單一具名實例轉發原始請求以保 LINE 簽章 raw body、LINE secret 由 Worker 注入容器環境）、`wrangler.jsonc`（container binding + durable object + migration，`max_instances=1`）、`tsconfig.json`。
- `package.json` 加 devDependencies（wrangler 4.110、@cloudflare/containers 0.3.7、workers-types 5.x、typescript）與 `cf:dev`/`cf:deploy`/`cf:tail` scripts。
- README + `docs/project.md` 更新部署路徑，README 增「Cloudflare Containers 部署」小節（含限制說明與 D1 取捨）。

## Commands / Validation

```bash
npx tsc --noEmit                                   # worker 型別檢查
docker build --platform linux/amd64 -t bridgeaid:local .
docker run -d -p 8099:8080 bridgeaid:local         # /healthz /demo/ /recommend 煙霧測試
uv run pytest -q                                   # 157 passed
npm run docs:refresh                               # docs guards ok
```

Result: pass（本機容器煙霧測試三端點皆正常；pytest 157 passed；docs guards 全綠）

## Detailed Notes

- 本機 8080 被既有 nginx 佔用，改用 host port 8099 測試；`/healthz` 回 `db_configured:false, line_configured:false`（degraded mode 如預期），6 條規則載入。
- 尚未執行且需人工前置：`wrangler login`（互動）、`wrangler secret put LINE_*`、`wrangler deploy`（需 Workers Paid 方案，容器佈建數分鐘）、部署後設定 LINE webhook URL。
- 已知限制：容器 `sleepAfter` 後記憶體 session／已排程提醒清空（無 DB 為本設計）；提醒僅在喚醒時送達。

## Addendum — 免費 live 部署（VPS + Cloudflare Tunnel）

改用免費路徑並實際上線（`https://bridgeaid.aionyx.me`）:

- VPS `aionyx-vps`（Debian 13 ARM64，疑似 Mac OrbStack VM；對外 IP 不可直連 → 走 Tunnel）。
- App：tar-over-ssh 傳 build context 到 `~/bridgeaid` → `docker build`（原生 arm64，Py 3.14）→ 容器 `bridgeaid` 發佈 `127.0.0.1:8091`（8080 被佔），`--restart unless-stopped`，degraded 模式。
- Tunnel：具名 `bridgeaid`（id `5a9d3bfc…c4e4`），DNS CNAME `bridgeaid.aionyx.me`，config 在 `/etc/cloudflared/config.yml`（ingress → localhost:8091），systemd `cloudflared` 服務 enabled（首次 install 誤讀 root home，改置 /etc/cloudflared 後成功）。
- 驗證：外網 `/healthz` 回 6 規則、`/demo/` 正常、CF-RAY=TPE。
- 待辦：LINE 尚未接（需注入 LINE_* env 重建容器 + 設 webhook）。詳見全域記憶 `live-deployment`。

## Commands / Validation (addendum)

```bash
tar czf - pyproject.toml uv.lock Dockerfile .dockerignore backend demo data | ssh aionyx-vps 'tar xzf - -C ~/bridgeaid'
ssh aionyx-vps 'cd ~/bridgeaid && docker build -t bridgeaid:latest . && docker run -d --name bridgeaid --restart unless-stopped -p 127.0.0.1:8091:8080 bridgeaid:latest'
cloudflared tunnel route dns bridgeaid bridgeaid.aionyx.me
sudo cloudflared service install     # config in /etc/cloudflared/
curl https://bridgeaid.aionyx.me/healthz
```

Result: pass（外網端到端通；cloudflared systemd active+enabled）

## Completed Markers

<!-- Use this exact format for completed index generation. -->

<!-- 基礎設施工作，非 TASK.NNN；不產生 completed 索引標記。 -->
