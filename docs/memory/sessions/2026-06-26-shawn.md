---
type: session_log
status: archive
priority: p3
updated: 2026-06-26
context_policy: historical
owner: shawn
---

# Session Log: 2026-06-26 - Shawn

## Summary

- Completed TASK.001 by locking the MVP scope to Taipei City + central nationwide services, replacing placeholder source URLs with official pages, and adding a sixth rule for Taipei low-income living support.
- Completed TASK.007 by adding 12 demo scenario fixtures and regression coverage for deterministic intent extraction, recommendation output, source trace URLs, needs-review flags, and conflict prompts.
- Completed TASK.008 by auditing the six MVP service rules against official pages and tightening over-broad demo eligibility rules.
- Completed TASK.009 by adding a dependency-free static Web demo shell served by FastAPI at `/demo/`.

## Actions Taken

- Read `CLAUDE.md`, team identity, current memory, active tasks, engineering principles, and targeted data/security/architecture docs.
- Verified official source pages for Taipei emergency aid, central rent subsidy, Taipei social housing, Taipei low-income living support, long-term care, and unemployment benefits.
- Updated `data/services/*.json` to use official source URLs and `last_checked_at: 2026-06-26`.
- Added `data/services/low_income_living_taipei.json` for the生活補助 / 邊緣戶 reminder slice.
- Documented the MVP service candidate scope in `docs/data.md`.
- Added a schema test that rejects placeholder `example.gov.tw` URLs in shipped service rules.
- Added `tests/fixtures/demo_scenarios.json` with 12 Week 7 demo situations across urgent aid, rent subsidy, social housing, low-income living support, long-term care, and unemployment.
- Added `tests/test_demo_scenarios.py` to assert expected parser fields, recommended service IDs, official source URLs, `needs_review`, and conflict pairs.
- Fixed deterministic parser coverage for `收入一般` / `一般家庭` / `一般戶`, `沒有工作`, and the `中低收入戶` vs `低收入戶` substring ordering.
- Re-audited official service pages on 2026-06-26. Kept Taipei emergency aid and Taipei low-income support active; marked central rent subsidy, Taipei social housing, and long-term care as `needs_review` because the stored rules are intentionally simplified.
- Replaced the rent subsidy source URL with the accessible NLMA 115-year official announcement, since it exposes eligibility and document requirements directly.
- Tightened Taipei emergency aid so income status alone no longer qualifies without an emergency event token.
- Tightened long-term care so caregiver status alone no longer qualifies without `care_need`.
- Changed Taipei social housing age threshold from 20 to 18 to match current adulthood-based wording.
- Added `demo/index.html`, `demo/styles.css`, `demo/app.js`, and `demo/bridgeaid-mark.svg`.
- Mounted `StaticFiles` at `/demo` in `app.main`.
- Added `tests/test_demo_ui.py` to verify the demo index and assets are served.

## Commands / Validation

```bash
rtk npm run team:status
rtk uv run ruff check .
rtk uv run pytest
rtk npm run docs:new-session
rtk uv run pytest tests/test_demo_scenarios.py -q
rtk uv run ruff check .
rtk uv run pytest
rtk uv run pytest tests/test_schema.py tests/test_demo_scenarios.py tests/test_evaluator.py tests/test_reminders_api.py -q
rtk uv run pytest tests/test_demo_ui.py -q
rtk uv run pytest
rtk powershell -NoProfile -Command "<start uvicorn on first free port from 8000 and verify /healthz + /demo/>"
```

Result: pass. Final `pytest` collected 93 tests and all passed; one existing Starlette/httpx deprecation warning remains. Local demo verification returned `/healthz` rules=6 and `/demo/` HTTP 200 on port 8000.

## Detailed Notes

- TASK.001 acceptance is represented in `docs/data.md` under "MVP Service Candidate Scope": main scenes are urgent aid, housing/living support, and edge-household reminders; geography is Taipei City + central nationwide services; service count is 6.
- `unemployment_assistance_central` keeps `status: needs_review` even with an official WDA source because the demo rule remains simplified and should not be presented as high confidence until human legal/policy review.
- Existing working tree already contained TASK.006/backend/docs changes before this session; those were left intact.
- TASK.007 intentionally avoided external LLM/Ollama, Redis/Celery, LINE push, and new dependencies because those remain approval- or ADR-gated. The fixture now gives a stable local safety net for Week 7 demo polish.
- TASK.008 audit findings: rent subsidy official rules require adult/eligible-minor status, no owned housing, income below thresholds, no overlapping housing assistance unless allowed, and valid lease/housing constraints; demo rule only captures lease + income token, so it remains `needs_review`. Taipei social housing requires adult status plus residence/study/work need, no owned housing, no existing social/public housing tenancy, income and property limits; demo rule only captures Taipei + adulthood, so it remains `needs_review`. Long-term care requires county care-management assessment and eligible care category; demo rule only captures care need, so it remains `needs_review`. Unemployment remains `needs_review` because non-voluntary separation and insurance seniority are not fully encoded.
- TASK.009 avoided a new frontend dependency and CORS complexity by serving same-origin static HTML/CSS/JS from FastAPI. The demo uses existing `/chat`, `/services`, `/services/{id}/source`, and `/reminders` endpoints.

## Completed Markers

<!-- Use this exact format for completed index generation. -->

## COMPLETED: TASK.001 - 題目收斂與 MVP 服務清單
## COMPLETED: TASK.007 - Demo 情境回歸測試（Week 7）
## COMPLETED: TASK.008 - MVP 服務資料準確性稽核
## COMPLETED: TASK.009 - Web Demo Shell（無新增依賴）
