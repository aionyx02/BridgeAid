---
type: session_log
status: archive
priority: p3
updated: 2026-07-04
context_policy: historical
owner: shawn
---

# Session Log: 2026-07-04 - Shawn

## Summary

- Commit `d2a3317`：TASK.013 政策審核（前一 session staged 的變更，GPG 重試成功）。
- TASK.014–016 全數實作：申請流程資料契約 + `/process` 端點、LINE Flex 流程卡 + 提醒 opt-in postback、對話式資料管理 + 1957 轉介。ADR-0007（proposed）。

## Actions Taken

- TASK.014：schema 加 `application_process[]`（`process_step`: name*/description*/url/url_title/deadline/deadline_at）；6 筆服務各 4 步官方流程；`build_recommendation` 對每筆 possible 附 steps；新端點 `GET /services/{id}/process`。
- TASK.015：新增 `backend/app/line/flex.py`（`service_bubble`/`results_flex_message`/`remind_consent_message`，上限 10 bubbles、label 截 20 字）；`client.py` 增 `reply_messages`（reply cap 5 則），文字回覆自動附 Flex carousel；新增 `backend/app/line/postback.py`（`parse_postback`、`reminder_time`：deadline_at−7 天 09:00、無期限 +7 天、下限明日 09:00）；`main.py` webhook 處理 postback 事件（remind → 同意確認 quickReply；remind_ok → consent=True 建 deadline 提醒；remind_no → 不建立；未知 action/service 靜默忽略）。
- TASK.016：`manager.py` 新增 `KIND_INFO`、`FIELD_LABELS`、指令攔截（`我的資料`/`修改<欄位>`/`清除資料`），在 pending-answer 與意圖抽取「之前」處理，指令不會被吞為答案；`_result_reply` 帶 profile，結果為空或 `income_status=near_threshold` 附 `HOTLINE_1957` 轉介文案。
- Web demo：`renderCards` 渲染流程步驟（含 ⏰ 時限、官方連結）與「提醒我申請」按鈕（預填提醒表單，consent 不代勾，滾動聚焦同意勾選框）；`submitChat` 對任何 kind 都渲染 options（info 指令選單可點）；styles.css 加 `.process*` 樣式。
- Docs：ADR-0007（proposed）；`data.md` 增 process 契約/端點/`kind:"info"`（精簡 JSON 範例騰空間，6144 限制內）；`active.md` 移出 010–013、收 014–016；`current.md` 更新。

## Commands / Validation

```bash
uv run ruff check .           # All checks passed
uv run ruff format --check .  # 43 files formatted（本輪先跑過 ruff format 全庫）
uv run pytest                 # 142 passed（新增 13 flex/postback + 8 profile-command）
```

Result: pass

## Detailed Notes

- GPG commit 首次逾時後，本 session 直接重試 `git commit` 一次即成功（pinentry 有時第一次逾時、重試即過）。
- `ruff format --check` 原本 6 檔未格式化（含先前 commit 的檔案），本輪 `ruff format .` 一次拉齊。
- postback data 為不可信輸入：只認 `action` 白名單與 `_rules_by_id()` 內存在的 service id。
- LINE quickReply 上限 13 項由 client 端 `[:13]` 保障；`我的資料` 的修改選項數 ≤ 9 欄位 + 清除資料，安全。
- `修改X` 會把欄位補進 `asked_fields`（防重問），會佔 MAX_QUESTIONS 額度——demo 規模可接受，之後若要豁免使用者主動修改，再分離計數。

## Completed Markers

## COMPLETED: TASK.014 - application_process 資料契約、6 服務流程步驟、GET /services/{id}/process，推薦結果附 steps
## COMPLETED: TASK.015 - LINE Flex 流程卡（官方入口/來源/提醒按鈕）+ postback opt-in 提醒流 + Web demo 流程渲染
## COMPLETED: TASK.016 - 我的資料/修改/清除資料 對話指令（kind=info）+ 1957 福利諮詢專線轉介（空結果與邊緣戶）
